namespace dlproj;

class GithubDownloader
{
  String projectName;
  String projectBranch;
  String localPath;

  String githubUrl = { "https://github.com/" + projectName + ".git" };
  String downloadDir = { localPath + File.separator + projectName + File.separator + projectBranch };
}
